import 'package:flutter/material.dart';

import 'package:bbg_project/Views/main_view.dart';

class App extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: MainView(),
    );
  }
}
